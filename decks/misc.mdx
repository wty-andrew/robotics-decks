import Tex from '@matejmazur/react-katex'
import { Embed } from 'theme-ui'

import { Hero, PseudoCode, FullScreenCodeBlock, CodeSurfer } from '../src/components'

import customTheme from '../theme'
export const theme = customTheme

<Hero title="Presentation Title" subtitle="Put your tagline here" />

---

# Speaker Notes

Press `Alt + p` to see speaker notes

<Notes>Only visible in presenter mode</Notes>

---

<FullScreenCodeBlock>

```jsx
// full screen code block

function HelloMessage({ name }) {
  return <div>Hello {name}</div>;
}

ReactDOM.render(
  <HelloMessage name="Taylor" />,
  document.getElementById('container')
);
```

</FullScreenCodeBlock>

---
import pythonSnippet from '!raw-loader!../src/snippets/sample.py'

<CodeSurfer
  language={'python'}
  code={pythonSnippet}
  customStyle={{ fontSize: '1.4rem' }}
  steps={[
    { title: 'Code Surfer Example' },
    {
      note: 'A class to represent a location in 2-D.',
      highlightLines: '4-7',
    },
    {
      title: 'Calculate euclidean distance',
      note: () => (
        <div>
          From Pythagorean Theorem:
          <Tex>{String.raw`\quad a^2 + b^2 = c^2`}</Tex>
        </div>
      ),
      highlightLines: '9,10',
    },
  ]}
/>

---
import reactSnippet from '!!raw-loader!../src/snippets/sample.js'
import reactJSXSnippet from '!!raw-loader!../src/snippets/sample2.js'

<CodeSurfer
  language={'javascript'}
  customStyle={{ fontSize: '1.4rem' }}
  title={'Diff Example'}
  steps={[
    {
      note: 'React without JSX',
      code: reactSnippet,
    },
    {
      note: 'JSX allows us to write HTML directly within the JavaScript code.',
      code: reactJSXSnippet,
      removedLines: '3,9',
      addedLines: '4,10',
    },
  ]}
/>

---

# KaTeX

<div>
  Inline:&nbsp;&nbsp;
  <Tex>{String.raw`x = \frac{-b\pm \sqrt{b^2 - 4ac}}{2a}`}</Tex>
</div>

<br />

<div>
  Block:
  <Tex block>{String.raw`x = \frac{-b\pm \sqrt{b^2 - 4ac}}{2a}`}</Tex>
</div>

---

# Pseudo Code

<PseudoCode noEnd captionCount={0}>
  {String.raw`
\begin{algorithm}
\caption{Fibonacci Series using Recursion}
\begin{algorithmic}
\FUNCTION{fib}{$n$}
    \IF{$n \le 1$}
        \RETURN $n$
    \ELSE
        \RETURN \CALL{fib}{$n - 1$} + \CALL{fib}{$n - 2$}
    \ENDIF
\ENDFUNCTION
\end{algorithmic}
\end{algorithm}
  `}
</PseudoCode>

---

# Embed

<Embed
  ratio={2.5}
  src="https://xstate.js.org/viz/?gist=9e4476d6312ac1bb29938d6c5e7f8f84&embed=1"
/>
